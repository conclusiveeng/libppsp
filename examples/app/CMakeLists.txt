set(CMAKE_INSTALL_RPATH "${CMAKE_INSTALL_PREFIX}/lib")
add_executable(peregrine peregrine.c)
set_target_properties(peregrine PROPERTIES OUTPUT_NAME "peregrine")
target_link_libraries(peregrine libperegrine)
install(TARGETS peregrine DESTINATION ${CMAKE_INSTALL_BINDIR})